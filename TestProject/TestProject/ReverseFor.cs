namespace TestProject
{
  public class ReverseFor
  {
    /// <summary>
    /// Reverses for loop.
    /// </summary>
    public void ReverseForLoop()
    {
      string pip = "123";

      for (int i = 0; i < pip.Length; i++)
      {
      }

      for (int i = 0; i < pip.Length; i++)
      {
      }

      for (int i = 10; i >= 0; i--)
      {
      }
    }
  }
}